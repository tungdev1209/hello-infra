apiVersion: v1
kind: Namespace
metadata:
  name: traefik
---
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: traefik
  namespace: traefik
spec:
  repo: https://helm.traefik.io/traefik
  chart: traefik
  version: "26.1.0"
  targetNamespace: traefik
  valuesContent: |-
    service:
      type: NodePort
    ports:
      web:
        nodePort: 80
