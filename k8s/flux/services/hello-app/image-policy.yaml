apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: hello-app-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: hello-app
  policy:
    semver:
      range: ">=1.0.0"
